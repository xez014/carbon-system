# 碳中和数字碳资产管理系统开发文档

## 1. 项目概述

### 1.1 项目简介
碳中和数字碳资产管理系统是一个基于微服务架构的数字化平台，旨在为企业和个人提供碳资产的全生命周期管理，包括碳配额管理、碳信用开发、碳交易、碳资讯等功能。

### 1.2 项目目标
- 实现碳资产的数字化管理
- 提供碳交易的便捷通道
- 促进碳资产开发和减排项目实施
- 提供碳市场资讯和数据分析

### 1.3 系统特点
- 采用微服务架构，服务独立部署，便于扩展和维护
- 前后端分离设计，提高开发效率和用户体验
- 统一响应格式，便于前端处理
- 支持多租户模式，满足不同用户需求

## 2. 技术架构

### 2.1 前端技术栈
- **框架**：Vue 3
- **构建工具**：Vite
- **UI组件库**：Element Plus
- **路由管理**：Vue Router 4
- **状态管理**：Pinia
- **HTTP请求**：Axios
- **图表库**：ECharts
- **样式预处理器**：Sass

### 2.2 后端技术栈
- **基础框架**：Spring Boot 3.1.10
- **微服务框架**：Spring Cloud 2022.0.5
- **微服务组件**：Spring Cloud Alibaba 2022.0.0.0-RC2
- **ORM框架**：MyBatis Plus
- **数据库**：MySQL 8
- **缓存**：Redis
- **服务注册与发现**：Nacos
- **开发语言**：Java 21
- **构建工具**：Maven

## 3. 系统架构

### 3.1 微服务架构
系统采用典型的微服务架构，各服务独立部署，通过Nacos进行服务注册与发现，通过Feign进行服务间通信。

```
carbon-system/
├── carbon-common          # 公共模块
├── service-consumer       # 网关服务 (8082)
├── service-provider       # 基础服务 (8081)
├── service-assets         # 碳资产服务 (8083)
├── service-trade          # 碳交易服务 (8084)
├── service-development    # 碳资产开发服务 (8085)
├── service-news           # 新闻资讯服务 (8086)
└── frontend               # 前端工程
```

### 3.2 服务职责
- **carbon-common**：公共实体类、工具类、异常定义、统一响应格式等
- **service-consumer**：服务消费者，作为API网关，转发前端请求到各微服务
- **service-provider**：基础服务，提供用户管理、权限控制等核心功能
- **service-assets**：碳资产服务，管理碳配额、碳信用等资产
- **service-trade**：碳交易服务，处理交易订单、市场行情等
- **service-development**：碳资产开发服务，管理减排项目、方法学等
- **service-news**：新闻资讯服务，提供碳市场相关资讯

## 4. 功能模块

### 4.1 仪表盘模块
- **功能描述**：提供系统概览和用户个人统计数据
- **核心接口**：
  - `GET /api/dashboard/summary`：获取仪表盘汇总数据（用户总数、企业认证数、碳资产总量等）
  - `GET /api/dashboard/user-stats`：获取用户个人统计数据（个人碳资产、交易次数等）
- **实现文件**：`service-consumer/src/main/java/com/example/consumer/controller/DashboardController.java`

### 4.2 账户管理模块
- **功能描述**：管理用户基本信息和企业信息
- **子模块**：
  - 基本信息管理
  - 企业信息管理
- **实现路径**：前端 `src/views/account/`

### 4.3 碳资产管理模块
- **功能描述**：管理碳配额和碳信用资产
- **子模块**：
  - 碳信用管理
  - 碳配额管理
- **实现路径**：前端 `src/views/assets/`，后端 `service-assets/`

### 4.4 碳资产开发模块
- **功能描述**：管理碳资产开发项目和方法学
- **子模块**：
  - 方法学管理
  - 开发项目管理
  - 开发工作台
- **核心接口**：
  - `GET /api/development/workbench/projects/active`：获取活跃项目列表
  - `POST /api/development/workbench/projects/{id}/initialize`：初始化项目任务
  - `PUT /api/development/workbench/projects/{id}/step`：更新项目进度
- **实现文件**：`service-development/src/main/java/com/example/development/controller/WorkbenchController.java`

### 4.5 碳交易行情模块
- **功能描述**：提供碳交易市场行情和交易功能，包括实时行情展示、历史行情查询、询报价管理和交易订单处理
- **子模块**：
  - 供需行情
  - 询报价
  - 交易所
  - 交易账户
- **核心接口**：
  - **行情管理**：
    - `GET /api/trade/quote/market/all`：获取所有资产类型的实时行情
    - `GET /api/trade/quote/market/{assetType}`：根据资产类型获取实时行情
    - `GET /api/trade/quote/history/daily`：获取日级历史行情
    - `GET /api/trade/quote/history/minute`：获取分钟级历史行情
  - **询报价管理**：
    - `GET /api/trade/quotes/market`：获取市场询报价单列表
    - `GET /api/trade/quotes/my`：获取我的询报价单
    - `GET /api/trade/quotes/{id}`：获取询价单详情
    - `POST /api/trade/quotes/publish`：发布询价单
    - `POST /api/trade/quotes/{id}/quote`：报价
    - `POST /api/trade/quotes/{id}/accept`：接受报价
    - `POST /api/trade/quotes/{id}/reject`：拒绝报价
    - `POST /api/trade/quotes/{id}/cancel`：取消询价单
  - **交易订单管理**：
    - `GET /api/trade/orders/market`：获取市场订单列表
    - `POST /api/trade/orders/publish`：发布交易订单
    - `POST /api/trade/orders/cancel/{orderId}`：取消订单
- **实现文件**：
  - 行情控制器：`service-trade/src/main/java/com/example/trade/controller/MarketQuoteController.java`
  - 询报价控制器：`service-trade/src/main/java/com/example/trade/controller/QuoteOrderController.java`
  - 数据库初始化：`service-trade/src/main/resources/init.sql`
- **实现路径**：前端 `src/views/market/`，后端 `service-trade/`

### 4.6 碳资讯模块
- **功能描述**：提供碳市场相关新闻和资讯
- **子模块**：
  - 资讯列表
  - 资讯详情
- **实现路径**：前端 `src/views/news/`，后端 `service-news/`

### 4.7 系统管理模块
- **功能描述**：管理系统配置和运营
- **子模块**：
  - 平台运营管理
  - 基础配置管理
  - 平台审批管理
- **实现路径**：前端 `src/views/system/`

## 5. 代码结构

### 5.1 后端代码结构
```
service-consumer/
├── src/main/java/com/example/consumer/
│   ├── controller/          # 控制器层
│   ├── entity/              # 实体类
│   ├── feign/               # Feign客户端（服务调用）
│   └── ConsumerApplication.java  # 应用入口
├── src/main/resources/
│   └── application.yml      # 配置文件
└── pom.xml                  # Maven配置

service-assets/
├── src/main/java/com/example/assets/
│   ├── controller/          # 控制器层
│   ├── entity/              # 实体类
│   ├── mapper/              # Mapper接口
│   ├── service/             # 服务层
│   └── AssetsApplication.java  # 应用入口
├── src/main/resources/
│   ├── application.yml      # 配置文件
│   └── init.sql             # 初始化脚本
└── pom.xml                  # Maven配置
```

### 5.2 前端代码结构
```
frontend/src/
├── api/                     # API接口定义
│   ├── assets.js            # 碳资产相关接口
│   ├── company.js           # 企业相关接口
│   ├── dashboard.js         # 仪表盘相关接口
│   ├── development.js       # 开发相关接口
│   ├── news.js              # 资讯相关接口
│   ├── trade.js             # 交易相关接口
│   └── user.js              # 用户相关接口
├── components/              # 公共组件
├── layout/                  # 布局组件
├── router/                  # 路由配置
├── store/                   # 状态管理
├── utils/                   # 工具函数
├── views/                   # 页面组件
├── App.vue                  # 根组件
├── main.js                  # 应用入口
└── style.css                # 全局样式
```

## 6. 开发环境搭建

### 6.1 后端环境
- **JDK**：Java 21
- **Maven**：3.6+（推荐3.8+）
- **MySQL**：8.0+ 
- **Redis**：5.0+ 
- **Nacos**：2.0+ 

### 6.2 前端环境
- **Node.js**：16.0+ 
- **npm**：8.0+ 

### 6.3 环境配置
1. 安装JDK 21并配置环境变量
2. 安装Maven并配置环境变量
3. 安装MySQL，创建数据库并执行初始化脚本
4. 安装Redis并启动服务
5. 安装Nacos并启动服务
6. 安装Node.js和npm
7. 克隆代码仓库

### 6.4 项目启动
- **后端服务**：
  1. 进入各服务目录（如service-consumer）
  2. 执行 `mvn clean install` 构建项目
  3. 执行 `mvn spring-boot:run` 启动服务

- **前端服务**：
  1. 进入frontend目录
  2. 执行 `npm install` 安装依赖
  3. 执行 `npm run dev` 启动开发服务器

## 7. API文档

### 7.1 统一响应格式
```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

- **code**：响应状态码（200表示成功，其他表示失败）
- **message**：响应信息
- **data**：响应数据

### 7.2 核心API列表

| 接口路径 | 方法 | 功能描述 | 所属服务 |
|---------|------|---------|--------|
| `/api/dashboard/summary` | GET | 获取仪表盘汇总数据 | service-consumer |
| `/api/dashboard/user-stats` | GET | 获取用户个人统计数据 | service-consumer |
| `/api/development/workbench/projects/active` | GET | 获取活跃项目列表 | service-development |
| `/api/development/workbench/projects/{id}/initialize` | POST | 初始化项目任务 | service-development |
| `/api/trade/quote/market/all` | GET | 获取所有资产类型的实时行情 | service-trade |
| `/api/trade/quote/market/{assetType}` | GET | 根据资产类型获取实时行情 | service-trade |
| `/api/trade/quote/history/daily` | GET | 获取日级历史行情 | service-trade |
| `/api/trade/quote/history/minute` | GET | 获取分钟级历史行情 | service-trade |
| `/api/trade/quotes/market` | GET | 获取市场询报价单列表 | service-trade |
| `/api/trade/quotes/publish` | POST | 发布询价单 | service-trade |
| `/api/trade/quotes/{id}/quote` | POST | 报价 | service-trade |
| `/api/trade/quotes/{id}/accept` | POST | 接受报价 | service-trade |
| `/api/trade/orders/market` | GET | 获取市场订单列表 | service-trade |
| `/api/trade/orders/publish` | POST | 发布交易订单 | service-trade |
| `/api/trade/orders/cancel/{orderId}` | POST | 取消订单 | service-trade |

## 8. 部署与维护

### 8.1 部署架构
- **开发环境**：单节点部署，各服务直接运行在开发机上
- **测试环境**：多节点部署，模拟生产环境
- **生产环境**：容器化部署（Docker），使用Kubernetes进行编排

### 8.2 维护建议
- 定期备份数据库
- 监控服务运行状态
- 及时更新依赖版本
- 记录系统日志便于问题排查

## 9. 开发规范

### 9.1 后端开发规范
- 采用Spring Boot 3.x + MyBatis Plus开发
- 遵循RESTful API设计规范
- 使用Lombok简化代码
- 异常处理采用统一异常拦截器
- 日志使用SLF4J + Logback

### 9.2 前端开发规范
- 采用Vue 3 + Element Plus开发
- 使用Composition API
- 组件化开发，提高复用性
- API接口统一管理在api目录下
- 状态管理使用Pinia

### 9.3 代码提交规范
- 提交信息使用中英文混合，清晰描述修改内容
- 分支管理：main（主分支）、dev（开发分支）、feature（功能分支）

## 10. 未来计划

### 10.1 功能扩展
- 增加碳排放核算功能
- 支持更多碳资产类型
- 增加数据分析和预测功能
- 支持移动端访问

### 10.2 技术优化
- 引入Elasticsearch实现全文搜索
- 增加消息队列（RocketMQ/Kafka）提高系统吞吐量
- 实现服务熔断和限流
- 优化数据库查询性能

### 10.3 安全性提升
- 增加接口鉴权和加密
- 实现细粒度权限控制
- 加强数据安全防护

---

**文档版本**：1.0.0
**编制日期**：2026-01-08
**编制人**：系统开发团队